{
  "name": "expense-reconciliation-app",
  "version": "1.0.0",
  "description": "Full-stack expense reconciliation application",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "uvicorn backend.api:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd frontend && npm install"
  },
  "keywords": ["expense", "reconciliation", "fastapi", "react"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
